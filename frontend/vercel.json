{
  "framework": null,
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "env": {
    "VITE_API_URL": {
      "description": "Backend API URL (e.g. https://review-pulse-api.onrender.com)"
    }
  }
}
